# @package _global_
defaults:
  - /trainer: default
  - /loader: default
  - /dataset: expression_profile
  - /task: regression
  - /optimizer: adamw
  - /scheduler: plateau
  - /callbacks: [base, checkpoint]

train:
  monitor: val/loss
  mode: min

encoder: id

# we need this for classification!
decoder:
  _name_: hap_token
  d_output: 1